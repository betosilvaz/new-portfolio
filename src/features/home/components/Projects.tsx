import { motion } from "framer-motion";

export default function Projects() {
  return (
    <motion.section 
      className="bg-[#fbfbfb] dark:bg-[#050505] text-[#050505] dark:text-white py-24"
    >
      <div className="flex flex-col items-center max-w-6xl gap-12">
        <motion.div>

        </motion.div>
        <motion.div>

        </motion.div>
        <motion.div>
          
        </motion.div>
      </div>
    </motion.section>
  );
}